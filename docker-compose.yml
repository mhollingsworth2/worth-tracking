version: '3.8'
services:
  app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - worth-tracking-data:/data
    environment:
      - NODE_ENV=production
      - PORT=5000
      - DATA_DIR=/data
    restart: unless-stopped

volumes:
  worth-tracking-data:
